# Antigravity & Agent Guidelines (AGENTS.md)

이 프로젝트(`euchs-new`)에서 작업하는 모든 AI 에이전트는 다음 규칙을 항상 준수합니다.

## 핵심 규칙
1. **추측 금지**: 파일 경로, 함수명, DB 컬럼을 임의 추측하지 말고 전수 grep 후 작업할 것.
2. **증상 은폐 금지**: 에러를 단순 try/catch로 덮거나 침묵하지 말 것.
3. **시크릿 하드코딩 금지**: API Key/Secret은 반드시 환경변수로 관리할 것.
4. **검증 후 Push**: 빌드 성공뿐 아니라 브라우저 실제 동작 확인 후 사용자 승인을 받아 push할 것.
5. **브라우저 확인 상황 시 로컬호스트 링크 필수 제공**:
   - 브라우저에서 직접 화면을 확인하거나 검증, 테스트해야 하는 모든 상황(화면 안내, 실측 확인 요청 등)에는 사용자가 즉시 클릭할 수 있도록 **현재 활성화된 로컬호스트 링크(예: `http://localhost:5175/...`)를 반드시 함께 제공**한다.
