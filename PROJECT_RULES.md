# EUCHS ERP 프로젝트 작업 규칙 (PROJECT_RULES.md)

이 파일은 euchs-new 프로젝트에서 코드 수정을 요청할 때마다 항상 적용되는 규칙입니다.
안티그래비티/Claude Code 등 어떤 AI 코딩 도구를 쓰든, 새 대화를 시작할 때 이 파일을 먼저 참고하도록 요청하세요.

---

## ⚠️ 엄격한 개발 원칙 (모든 작업에 항상 적용)

1. **추측 금지**: 파일 경로나 함수명을 임의로 추측하여 수정하지 않는다. 반드시 실제 소스코드 전체를 grep하여 정확한 위치를 특정한 후 작업한다.
2. **증상 은폐 금지**: 에러를 단순 `try/catch`나 빈 함수로 감싸서 표면적으로만 모면하지 않는다. 근본 원인을 규명하여 해결한다.
3. **시크릿 하드코딩 금지**: API Key/Secret 등 인증 정보를 소스코드에 절대 평문으로 넣지 않는다. 환경변수로만 관리한다.
4. **파라미터 임의 추가/삭제 금지**: 확인되지 않은 API 파라미터를 근거 없이 추가하거나 삭제하지 않는다. 공식 문서나 검증된 호출부를 근거로만 구성한다.
5. **검증 후 push**: 로컬(또는 실사이트) 환경에서 실제로 정상 동작하는 것을 직접 확인하기 전에는 `git push origin main`을 실행하지 않는다. "빌드 성공"은 검증이 아니다 — 반드시 브라우저에서 실제 동작 확인 후 push.
6. **회귀 체크 필수**: 수정 후에는 이번에 건드리지 않은 기존 정상 기능(검색, 이미지검색, 상세조회, 장바구니, 결제 등)이 함께 깨지지 않았는지 확인한다.

---

## ✅ 확정된 기술 스펙 (재확인 없이 바로 사용 가능)

### OneBound 1688 API
- 담당 매니저: 周俊Turbo (QQ: 3142401606 / 위챗: onebound1997)
- **반드시 `1688global` 엔드포인트를 사용할 것** (구버전 `/1688/` 경로는 만료/권한없음 에러 발생함)
  - 상세조회: `https://api-gw.onebound.cn/1688global/item_get/?key=${OB_KEY}&secret=${OB_SECRET}&num_iid=...&result_type=json`
  - 텍스트검색: `1688global` 계열 사용
  - 이미지검색: `https://api-gw.onebound.cn/1688global/item_search_img/?key=${OB_KEY}&secret=${OB_SECRET}&imgid=<이미지의 실제 공개 URL>&cache=no&lang=zh-CN`
    - ⚠️ `imgid`는 숫자 ID가 아니라 **이미지 파일의 실제 공개 URL 문자열**이어야 함
    - `upload_img`(숫자 imgid 발급) 단계는 1688global 방식에서는 불필요
- `session` / `session_id` 파라미터는 **불필요** (넣으면 오히려 에러 유발 가능성 있음, 근거 없이 추가 금지)
- Key/Secret은 반드시 `ONEBOUND_KEY`, `ONEBOUND_SECRET` 환경변수로만 관리

### 로컬 개발 환경 주의사항
- `vite.config.js`의 인라인 프록시 미들웨어가 Vercel의 `api/*.js` 프록시와 별개로 동작할 수 있음
- 로컬(`npm run dev`)과 프로덕션(Vercel)에서 실제로 호출되는 코드 경로가 다를 수 있으므로, 문제 발생 시 둘 다 확인할 것
- 로컬 개발 서버 요청은 Node.js 미들웨어 레벨에서 처리될 경우 브라우저 Network 탭(Fetch/XHR)에 기록되지 않을 수 있음 — "요청이 안 보인다"고 API가 안 불린 게 아닐 수 있음
- `vite.config.js` 변경은 dev 서버 재시작 필요 (핫리로드로 반영 안 됨)

### 브라우저 캐시 주의사항
- 이 사이트는 검색 결과를 `localStorage`/`sessionStorage`에 캐싱함
- 과거 실패했던 빈 결과가 캐시로 남아있으면, 코드를 고쳐도 화면에 캐시된 빈 결과가 계속 보일 수 있음
- 강력 새로고침(Ctrl+F5)으로는 지워지지 않음 — F12 → Application → Storage → Clear site data 필요
- 실제 신규 고객은 캐시가 없으므로 이 문제와 무관함 (개발자 확인용 이슈)

---

## 📋 작업 순서 템플릿

1. 사전 조사 (grep으로 관련 파일/함수 전부 특정, 로컬+프로덕션 양쪽 코드 경로 확인)
2. 원인 규명 (케이스별로 원인 정리 — 추측 아닌 근거 기반)
3. 코드 수정 (위 확정 스펙 준수)
4. `npm run build`로 컴파일 에러 0건 확인
5. **로컬 또는 실사이트에서 브라우저로 직접 동작 확인** (필요시 캐시 삭제 후 확인)
6. 회귀 체크 (기존 정상 기능들 확인)
7. 커밋 메시지에 원인과 해결 방법 명시 후 push
